public class Operator5
{
	public static void main(String[] args)
	{
		intx = 2, y = -2;
		intresult;

		result = x > 0 ? x : -x;
		System.out.println( "x的值為："+ x + "，它的絕對值為："+ result);

		result = y > 0 ? y : -y;
		System.out.println("y的值為："+ y + "，它的絕對值為："+ result);
	}
}